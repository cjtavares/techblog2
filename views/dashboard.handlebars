 <section>
   {{#each posts as |post|}}
      <div>
        <h4>
          <a class="name title " href="/character/{{id}}">{{post.post_title}}</a>
        </h4>
        <p>{{format_date post.createdAt}}</p>
        
      </div>
    {{/each}}
  </section>
  