 <section>
   {{#each posts as |post|}}
      <div>
        <h4>
          {{post.post_title}}.
        </h4>
        <p> {{format_date post.createdAt}}</p>
      
      </div>
    {{/each}}
  </section>
  